{
  "name": "rc-mcp-read-messages",
  "version": "1.0.0",
  "description": "Minimal MCP server for Rocket.Chat: read-messages",
  "mcpServers": {
    "rc-mcp-read-messages": {
      "command": "node",
      "args": [
        "./dist/server.js"
      ],
      "env": {
        "RC_URL": "${RC_URL}",
        "RC_USER": "${RC_USER}",
        "RC_PASSWORD": "${RC_PASSWORD}"
      }
    }
  }
}