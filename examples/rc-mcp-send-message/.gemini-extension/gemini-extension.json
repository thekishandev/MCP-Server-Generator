{
  "name": "rc-mcp-send-message",
  "version": "1.0.0",
  "description": "Minimal MCP server for Rocket.Chat: send-message",
  "mcpServers": {
    "rc-mcp-send-message": {
      "command": "node",
      "args": [
        "./dist/server.js"
      ],
      "env": {
        "RC_URL": "${RC_URL}",
        "RC_USER": "${RC_USER}",
        "RC_PASSWORD": "${RC_PASSWORD}"
      }
    }
  }
}