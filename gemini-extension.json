{
  "name": "rc-mcp-generator",
  "version": "0.2.0",
  "description": "Generate minimal MCP servers for Rocket.Chat with only the APIs you need",
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "rcMcpGenerator": {
      "command": "node",
      "args": ["${extensionPath}${/}dist${/}extension${/}server.js"],
      "cwd": "${extensionPath}"
    }
  }
}
